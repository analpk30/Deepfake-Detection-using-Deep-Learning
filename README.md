# Deepfake Detection using Deep Learning

Refactored service structure with:
- `app.py`: all core logic (training, evaluation, Streamlit UI, and FastAPI service).
- `constants.py`: all shared constants.
- `tests/test_app.py` and `tests/test_constants.py`: consolidated tests.
